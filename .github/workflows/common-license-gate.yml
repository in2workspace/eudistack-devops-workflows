name: common-license-gate

on:
  workflow_call:
    inputs:
      setup_java:
        description: Set up Java and make the Gradle wrapper executable
        required: false
        type: boolean
        default: true
      java_version:
        description: Java version used to generate the SBOM
        required: false
        type: string
        default: '25'
      node_version:
        description: Node.js version used by the license evaluator
        required: false
        type: string
        default: '22'
      sbom_command:
        description: Command used to generate the CycloneDX SBOM
        required: false
        type: string
        default: ./gradlew cyclonedxBom
      install_command:
        description: Optional dependency installation command
        required: false
        type: string
        default: ''
      sbom_path:
        description: Generated CycloneDX SBOM path
        required: false
        type: string
        default: build/reports/sbom/sbom.json

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  changes:
    name: Detect license-relevant changes
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    outputs:
      relevant: ${{ steps.filter.outputs.relevant }}
    steps:
      - name: Filter changed paths
        id: filter
        uses: dorny/paths-filter@ceb8a2b8f2d89434be7ff52d3de7ec3738c5cc9d # v4.0.3
        with:
          predicate-quantifier: 'every'
          filters: |
            relevant:
              - '**'
              - '!**/*.md'
              - '!.github/**'
              - '.github/license-policy.json'
              - '.github/license-exceptions.json'
              - '!docs/**'
              - '!LICENSE'
              - '!.gitignore'

  license-gate:
    name: License Compliance Check
    needs: changes
    if: ${{ !cancelled() && needs.changes.outputs.relevant != 'false' }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Set up shared delivery scripts
        uses: in2workspace/eudistack-devops-workflows/.github/actions/setup-scripts@3ab05aa7bea7a29077cc0ba27ea1993d5c63cdf5

      - name: Set up JDK 25
        if: inputs.setup_java
        uses: actions/setup-java@dd06d9cba3e5552c54d9f8ea23572deb30010f7c # v6
        with:
          distribution: 'temurin'
          java-version: ${{ inputs.java_version }}
          cache: 'gradle'

      - name: Make Gradlew Executable
        if: inputs.setup_java
        run: chmod +x ./gradlew

      - name: Set up Node.js 22
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: ${{ inputs.node_version }}

      - name: Verify the license gate itself
        run: node --test "${EUDISTACK_DEVOPS_COMMON_SCRIPTS}/license-gate.test.mjs"

      - name: Install dependencies
        if: inputs.install_command != ''
        run: ${{ inputs.install_command }}

      - name: Generate CycloneDX SBOM
        run: ${{ inputs.sbom_command }}

      - name: Evaluate licenses
        run: node "${EUDISTACK_DEVOPS_COMMON_SCRIPTS}/license-gate.mjs" --sbom-path "${{ inputs.sbom_path }}"
